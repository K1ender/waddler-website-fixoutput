import CodeTabs from "@mdx/CodeTabs.astro";


<CodeTabs items={["node-postgres", "node-postgres with config", "your node-postgres driver", "pg-query-stream extension"]}>
```typescript copy
import 'dotenv/config';
import { waddler } from 'waddler/node-postgres';

const sql = waddler(process.env.DATABASE_URL!);
```
```typescript copy
import 'dotenv/config';
import { waddler } from 'waddler/node-postgres';

// You can specify any property from the node-postgres connection options
const sql = waddler({ 
  connection: { 
    connectionString: process.env.DATABASE_URL!,
    ssl: true
  }
});
```
```typescript copy
import 'dotenv/config';
import { waddler } from 'waddler/node-postgres';
import pg from 'pg';

const { Pool } = pg;

const pool = new Pool({
  connectionString: process.env.DATABASE_URL!,
});
const sql = waddler({ client: pool });
```
```typescript copy
import 'dotenv/config';
import { waddler } from 'waddler/node-postgres';
import { queryStream } from 'waddler/extensions/pg-query-stream';
import pg from 'pg';

const { Pool } = pg;

const pool = new Pool({
  connectionString: process.env.DATABASE_URL!,
});
const sql = waddler({ client: pool, extensions: [queryStream()] });
```
</CodeTabs>
